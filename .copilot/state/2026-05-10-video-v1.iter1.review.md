# Review — video-v1 · iteration 1

> Reviewer: Principal · Date: 2026-05-10
> Plan: [.copilot/state/2026-05-10-video-v1.plan.md](2026-05-10-video-v1.plan.md)
> Security: [.copilot/state/2026-05-10-video-v1.iter1.security.md](2026-05-10-video-v1.iter1.security.md)

## Decision
- [x] Approve
- [ ] Approve with nits
- [ ] Request changes
- [ ] Reject & replan

## Scores (1–5)
| Dimension       | Score | Notes |
|-----------------|-------|-------|
| Correctness     | 5     | All 8 ACs satisfied. Flag default `false` proven by test. Cross-team isolation proven on top-level (`VideoAsset`) and child (`VideoTimelineEvent`). |
| Tests           | 4     | 7 new tests, +128 total (was 121). Empty page is asserted; flag flips both ways; non-member is forbidden. Could add a model-snapshot golden test later but premature for skeleton. |
| Security        | 5     | All three iter-1 security adjustments landed in the same diff: (a) `IObjectStore.GetSignedReadUrlAsync(viewerUserId, expiresAt)` carries the HMAC contract; (b) `VideoJob` carries `TeamId` + `TraceParent`; (c) second isolation test on `VideoTimelineEvent`. Standing rules captured on the entity (welfare scrub TODO on `VideoTimelineEvent.Body`). |
| Readability     | 5     | Module is one folder, every file has a doc comment that says why it exists. Naming matches the architecture doc 1:1. |
| Design          | 5     | Module-internal in `Features/Video/`, additive only. No DI registration of impls — interfaces only, so V1 cannot accidentally start serving real bytes. Soft-delete pattern matches the rest of the codebase. |
| Performance     | 4     | Listing query has `(TeamId, CreatedAt)` index and `Take(0)` projection — cheap by construction. Nothing to optimise yet. |
| Operability     | 4     | Migration applies on startup via existing `MigrateAsync` block. No new metrics/traces yet — fine for skeleton; arch doc lists the metrics V2 will add (`video.uploads.bytes` etc.). |
| Reversibility   | 5     | Single-revert. Tables are empty by V1 design, so `ef database update <previous>` is loss-free. Flag default `false` means no env behaviour change on revert. |
| **Total / 40**  | **37**| |

## Must-fix (blocks merge)
None.

## Nits (follow-up ok)
1. Add `Features:Video:Enabled = false` explicitly to the prod Helm values in V2's PR, alongside the first env that wants it `true` for canary.
2. The migration timestamp `20260510184316` is wall-clock-locally-correct — fine on a single-developer repo, but the V2 backend should re-stamp on rebase if `main` has acquired any new migrations.
3. Consider promoting `VideoEnums.cs` doc-comment about welfare scrub into a unit test in V5 ("any list endpoint that returns timeline events scrubs `WelfareFlag` body for non-welfare viewers").

## Iteration delta
- **Better than last loop:** First slice in this repo to enter via the planner→security→backend→tester→reviewer flow before any code was written. Security review surfaced 3 small contract changes (HMAC viewer binding, `VideoJob` shape, second isolation test) that would have been painful to retrofit later — caught at zero cost.
- **Must improve next loop:** V2 must add an explicit perf budget for the upload endpoint (per-team byte quota + per-IP rate limit), and the reviewer should ask for the metrics list at plan-time rather than waiting for the diff.

## Quality gates
- [x] Tests pass — `dotnet test` 128/128 green.
- [x] Lint clean — build emits 0 warnings on the new code (3 pre-existing CS8604 warnings in unrelated test files, untouched).
- [x] Security scan clean — no new dependencies, no high/critical findings.
- [x] Plan acceptance criteria all covered by tests:
  - AC1 module folder layout — visible in diff.
  - AC2 ten entities — `VideoModelRegistrationTests.Model_registers_all_ten_video_entities`.
  - AC3 DbSets + (TeamId, …) indexes — `VideoModelRegistrationTests.Every_video_entity_has_a_TeamId_leading_index`.
  - AC4 migration applies — verified by full test run on InMemory, design-time generation succeeded against Postgres.
  - AC5 `IObjectStore`/`IVideoJobQueue` interfaces only, no DI registration — verified by file diff (no `AddSingleton<IObjectStore`/`IVideoJobQueue>` in `Program.cs`).
  - AC6 controller flag-gates: `List_returns_404_when_feature_disabled_by_default`, `List_returns_empty_page_when_feature_enabled_for_team_member`, `List_returns_403_for_non_member_when_feature_enabled`.
  - AC7 cross-team isolation: `List_for_team_B_does_not_see_team_A_VideoAsset` + `VideoTimelineEvent_is_isolated_per_team_at_storage_layer`.
  - AC8 build clean + test green — confirmed.

## Handoff for next slice
Backend may proceed to plan V2 (real upload endpoint + `LocalFsObjectStore` + first ffprobe pipeline stage) when the user is ready. Security must review V2 plan before backend starts — V2 introduces the first untrusted byte stream into the API.
