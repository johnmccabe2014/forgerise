# Trivy ignore file
# Format: one CVE/GHSA ID per line. Optionally `<id> exp:YYYY-MM-DD` to expire.
#
# IMPORTANT: every entry MUST link to a tracking issue and have an expiry.
#
# (currently empty — Next 16 + React 19 upgrade on 2026-05-09 cleared the
#  Next 14 picomatch and Next.js CVEs that previously lived here.)
